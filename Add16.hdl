CHIP Add16 {
    IN a[16], b[16];
    OUT out[16];

    PARTS:
    HalfAdder(a=a[0], b=b[0], sum=out[0], carry=c);
    // ... (Other FullAdder and HalfAdder calls)

    // Output the test results
    for (i = 0; i < 16; i = i + 1) begin
        $display("Test %d: a = %d, b = %d, out = %d", i, a[i], b[i], out[i]);
    end}