CHIP FullAdder {
    IN a, b, c;
    OUT sum, carry;

    PARTS:
    Xor(a=a, b=b, out=sum_ab);
    Xor(a=sum_ab, b=c, out=sum);
    And(a=a, b=b, out=and1);
    And(a=sum_ab, b=c, out=and2);
    Or(a=and1, b=and2, out=carry);
}